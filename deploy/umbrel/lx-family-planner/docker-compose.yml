version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: lx-family-planner_server_1
      APP_PORT: 3001
      PROXY_AUTH_ADD: "false"

  server:
    image: ghcr.io/laxxx-lab/lx-family-planner:1.21.0@sha256:2b3f0e6d9c83ecc2a5b9bc8fa27babee97dbf450df040696b876ca40baa23e4e
    user: "1000:1000"
    restart: on-failure
    stop_grace_period: 15s
    environment:
      NODE_ENV: production
      PORT: 3001
      TZ: Europe/Berlin
      APP_SECRET: "${APP_SEED}"
      SESSION_COOKIE_SECURE: auto
      DATABASE_FILE: /app/data/family_planner.sqlite
      LEGACY_DATABASE_FILE: /app/data/family_db.json
    volumes:
      - "${APP_DATA_DIR}/data:/app/data"
      - "${APP_DATA_DIR}/backups:/app/backups"
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
